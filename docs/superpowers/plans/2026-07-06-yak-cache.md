# yak 翻訳結果キャッシュ Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** diskcache + platformdirs で翻訳・辞書結果を永続キャッシュし、`--no-cache` / `--clear-cache` オプションを追加する。

**Architecture:** 任意のバックエンドを包む `CachingBackend` ラッパー(`Translator`/`DictionaryProvider` 両対応)を `src/yak/cache.py` に追加し、ファクトリ `create_backend` が `OpenAIBackend` を包んで返す。キーは `(モード, namespace, text, from, to, extra)` のタプル、値は Pydantic の `model_dump()` dict。

**Tech Stack:** diskcache, platformdirs(追加)+ 既存スタック(click, pydantic, pytest, mypy, ruff)

**Spec:** `docs/superpowers/specs/2026-07-06-yak-cache-design.md`

## Global Constraints

- 依存追加は `uv add`(diskcache, platformdirs)
- キャッシュディレクトリは `platformdirs.user_cache_dir("yak")`
- サイズ上限 100MB、TTL なし
- キー: `("translate" | "lookup", namespace, text, from_lang, to_lang, extra_instruction)`。namespace は `f"openai:{model}"`
- 値: `model_dump()` した dict を保存し `model_validate()` で復元(オブジェクトを直接 pickle しない)
- `--no-cache` は読み取りのみスキップし、結果は保存する
- `--clear-cache` は入力・バックエンド生成より先に判定し、API キー不要で
  `キャッシュをクリアしました (N 件)` を表示して正常終了する
- 辞書モード非対応エラーのメッセージは既存と同一:
  `this backend does not support dictionary mode`
- 各タスク完了時に `make check`(mypy + ruff)が通ること
- コミット時は pre-commit フックが走る。フックがファイルを修正してコミットが
  失敗したら、`git add` し直して再コミットする

---

### Task 1: CachingBackend とキャッシュユーティリティ

**Files:**
- Modify: `pyproject.toml`(uv add 経由 + mypy override 追記)
- Create: `src/yak/cache.py`
- Modify: `docs/superpowers/specs/2026-07-06-yak-cache-design.md`(委譲の文言修正)
- Test: `tests/test_cache.py`

**Interfaces:**
- Consumes: `yak.backends.base.Translator` / `DictionaryProvider`(runtime_checkable Protocol)、
  `yak.models.TranslationResult` / `DictionaryResult`、`yak.errors.YakError`、
  テスト用に `tests.test_cli.FakeBackend`(translate/lookup を記録するフェイク)と
  `tests.test_interactive.TranslateOnlyBackend`(translate のみ持つフェイク)
- Produces: `yak.cache.cache_directory() -> Path`
- Produces: `yak.cache.open_cache() -> diskcache.Cache`
- Produces: `yak.cache.clear_cache() -> int`(削除件数を返す)
- Produces: `yak.cache.CachingBackend(inner: Translator | DictionaryProvider, cache: diskcache.Cache, *, namespace: str, read_enabled: bool = True)` —
  `translate()` / `lookup()` は Protocol と同一シグネチャ

- [ ] **Step 1: 依存を追加する**

```bash
uv add diskcache platformdirs
```

Expected: `pyproject.toml` の dependencies に `diskcache` と `platformdirs` が追加される。

- [ ] **Step 2: mypy override を追記する**

diskcache は型情報(py.typed)を持たないため、`pyproject.toml` の
`[tool.mypy]` セクションの後に追記:

```toml
[[tool.mypy.overrides]]
module = "diskcache.*"
ignore_missing_imports = true
```

- [ ] **Step 3: 失敗するテストを書く**

`tests/test_cache.py` を作成:

```python
from pathlib import Path

import diskcache
import pytest

from tests.test_cli import FakeBackend
from tests.test_interactive import TranslateOnlyBackend
from yak.cache import CachingBackend
from yak.errors import YakError


def _make_cache(tmp_path: Path) -> diskcache.Cache:
    return diskcache.Cache(str(tmp_path / "cache"))


def test_second_translate_hits_cache(tmp_path: Path) -> None:
    backend = FakeBackend()
    caching = CachingBackend(backend, _make_cache(tmp_path), namespace="test")
    first = caching.translate("hello", None, None, None)
    second = caching.translate("hello", None, None, None)
    assert first == second
    assert len(backend.translate_calls) == 1


def test_second_lookup_hits_cache(tmp_path: Path) -> None:
    backend = FakeBackend()
    caching = CachingBackend(backend, _make_cache(tmp_path), namespace="test")
    first = caching.lookup("cat", None, None, None)
    second = caching.lookup("cat", None, None, None)
    assert first == second
    assert len(backend.lookup_calls) == 1


def test_translate_and_lookup_use_separate_entries(tmp_path: Path) -> None:
    backend = FakeBackend()
    caching = CachingBackend(backend, _make_cache(tmp_path), namespace="test")
    caching.translate("cat", None, None, None)
    caching.lookup("cat", None, None, None)
    assert len(backend.translate_calls) == 1
    assert len(backend.lookup_calls) == 1


def test_different_languages_are_separate_entries(tmp_path: Path) -> None:
    backend = FakeBackend()
    caching = CachingBackend(backend, _make_cache(tmp_path), namespace="test")
    caching.translate("hello", None, "French", None)
    caching.translate("hello", None, "German", None)
    assert len(backend.translate_calls) == 2


def test_different_extra_instructions_are_separate_entries(tmp_path: Path) -> None:
    backend = FakeBackend()
    caching = CachingBackend(backend, _make_cache(tmp_path), namespace="test")
    caching.translate("hello", None, None, None)
    caching.translate("hello", None, None, "Use polite form.")
    assert len(backend.translate_calls) == 2


def test_different_namespaces_are_separate_entries(tmp_path: Path) -> None:
    backend = FakeBackend()
    cache = _make_cache(tmp_path)
    a = CachingBackend(backend, cache, namespace="openai:model-a")
    b = CachingBackend(backend, cache, namespace="openai:model-b")
    a.translate("hello", None, None, None)
    b.translate("hello", None, None, None)
    assert len(backend.translate_calls) == 2


def test_read_disabled_always_calls_inner_but_still_writes(tmp_path: Path) -> None:
    backend = FakeBackend()
    cache = _make_cache(tmp_path)
    no_read = CachingBackend(
        backend, cache, namespace="test", read_enabled=False
    )
    no_read.translate("hello", None, None, None)
    no_read.translate("hello", None, None, None)
    assert len(backend.translate_calls) == 2
    reader = CachingBackend(backend, cache, namespace="test")
    reader.translate("hello", None, None, None)
    assert len(backend.translate_calls) == 2


def test_lookup_with_translate_only_inner_raises(tmp_path: Path) -> None:
    caching = CachingBackend(
        TranslateOnlyBackend(), _make_cache(tmp_path), namespace="test"
    )
    with pytest.raises(YakError, match="dictionary mode"):
        caching.lookup("word", None, None, None)
```

- [ ] **Step 4: テストが失敗することを確認する**

Run: `uv run pytest tests/test_cache.py -v`
Expected: FAIL(`ModuleNotFoundError: No module named 'yak.cache'`)

- [ ] **Step 5: cache.py を実装する**

`src/yak/cache.py` を作成:

```python
from pathlib import Path

import diskcache
from platformdirs import user_cache_dir

from yak.backends.base import DictionaryProvider, Translator
from yak.errors import YakError
from yak.models import DictionaryResult, TranslationResult

CACHE_SIZE_LIMIT = 100 * 1024 * 1024  # 100MB


def cache_directory() -> Path:
    return Path(user_cache_dir("yak"))


def open_cache() -> diskcache.Cache:
    return diskcache.Cache(str(cache_directory()), size_limit=CACHE_SIZE_LIMIT)


def clear_cache() -> int:
    with open_cache() as cache:
        count: int = cache.clear()
        return count


class CachingBackend:
    """任意のバックエンドを包み、結果を diskcache に永続キャッシュするラッパー。

    値は model_dump() した dict で保存する(クラス定義の pickle に依存させない)。
    """

    def __init__(
        self,
        inner: Translator | DictionaryProvider,
        cache: diskcache.Cache,
        *,
        namespace: str,
        read_enabled: bool = True,
    ) -> None:
        self._inner = inner
        self._cache = cache
        self._namespace = namespace
        self._read_enabled = read_enabled

    def translate(
        self,
        text: str,
        from_lang: str | None,
        to_lang: str | None,
        extra_instruction: str | None,
    ) -> TranslationResult:
        key = (
            "translate",
            self._namespace,
            text,
            from_lang,
            to_lang,
            extra_instruction,
        )
        if self._read_enabled:
            cached = self._cache.get(key)
            if cached is not None:
                return TranslationResult.model_validate(cached)
        if not isinstance(self._inner, Translator):
            raise YakError("this backend does not support translation")
        result = self._inner.translate(text, from_lang, to_lang, extra_instruction)
        self._cache[key] = result.model_dump()
        return result

    def lookup(
        self,
        text: str,
        from_lang: str | None,
        to_lang: str | None,
        extra_instruction: str | None,
    ) -> DictionaryResult:
        key = (
            "lookup",
            self._namespace,
            text,
            from_lang,
            to_lang,
            extra_instruction,
        )
        if self._read_enabled:
            cached = self._cache.get(key)
            if cached is not None:
                return DictionaryResult.model_validate(cached)
        if not isinstance(self._inner, DictionaryProvider):
            raise YakError("this backend does not support dictionary mode")
        result = self._inner.lookup(text, from_lang, to_lang, extra_instruction)
        self._cache[key] = result.model_dump()
        return result
```

- [ ] **Step 6: テストが通ることを確認する**

Run: `uv run pytest tests/test_cache.py -v`
Expected: PASS(8 passed)

Run: `make check`
Expected: mypy / ruff ともエラーなし

Run: `uv run pytest`
Expected: 全テスト PASS(既存 29 + 新規 8 = 37)

- [ ] **Step 7: spec の委譲文言を修正する**

`docs/superpowers/specs/2026-07-06-yak-cache-design.md` の以下の 3 行:

```
- inner が対象プロトコルを実装していない場合の扱い(辞書モード非対応エラー)は
  既存どおり CLI / InteractiveSession 側のガードに任せる。CachingBackend は
  inner にそのまま委譲する
```

を次に置き換える:

```
- CachingBackend 自体は両プロトコルのメソッドを持つため、CLI /
  InteractiveSession の isinstance ガードはラッパーに対して常に通る。
  そのため inner が対象プロトコルを実装していない場合の検出は
  CachingBackend 側で行い、既存と同一メッセージの YakError を送出する
```

- [ ] **Step 8: コミット**

```bash
git add pyproject.toml uv.lock src/yak/cache.py tests/test_cache.py \
        docs/superpowers/specs/2026-07-06-yak-cache-design.md
git commit -m "feat: add disk-backed caching backend wrapper"
```

---

### Task 2: CLI 統合(--no-cache / --clear-cache)と README

**Files:**
- Modify: `src/yak/main.py`
- Modify: `tests/test_cli.py`
- Modify: `README.md`

**Interfaces:**
- Consumes: `yak.cache.CachingBackend`, `yak.cache.open_cache`, `yak.cache.clear_cache`(Task 1)
- Produces: `yak.main.create_backend(model: str, *, read_cache: bool = True) -> CachingBackend`
  (シグネチャ変更 — 既存のテストフィクスチャも追随させる)

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_cli.py` を修正する。まず import に `Path` を追加:

```python
from pathlib import Path
from typing import Any
```

既存の `fake_backend` フィクスチャの monkeypatch を、新しい
`read_cache` キーワード引数を受けられるよう変更:

```python
@pytest.fixture
def fake_backend(monkeypatch: pytest.MonkeyPatch) -> FakeBackend:
    backend = FakeBackend()
    monkeypatch.setattr(
        "yak.main.create_backend", lambda model, **kwargs: backend
    )
    return backend
```

ファイル末尾に新テストを追加:

```python
def _capture_create_backend(
    monkeypatch: pytest.MonkeyPatch, captured: dict[str, Any]
) -> FakeBackend:
    backend = FakeBackend()

    def fake_create(model: str, *, read_cache: bool = True) -> FakeBackend:
        captured["read_cache"] = read_cache
        return backend

    monkeypatch.setattr("yak.main.create_backend", fake_create)
    return backend


def test_no_cache_disables_cache_read(monkeypatch: pytest.MonkeyPatch) -> None:
    captured: dict[str, Any] = {}
    _capture_create_backend(monkeypatch, captured)
    result = CliRunner().invoke(main, ["--no-cache", "hello"])
    assert result.exit_code == 0
    assert captured["read_cache"] is False


def test_cache_read_enabled_by_default(monkeypatch: pytest.MonkeyPatch) -> None:
    captured: dict[str, Any] = {}
    _capture_create_backend(monkeypatch, captured)
    result = CliRunner().invoke(main, ["hello"])
    assert result.exit_code == 0
    assert captured["read_cache"] is True


def test_clear_cache_clears_and_exits(
    monkeypatch: pytest.MonkeyPatch, tmp_path: Path
) -> None:
    monkeypatch.delenv("OPENAI_API_KEY_FOR_YAK", raising=False)
    monkeypatch.setattr("yak.cache.cache_directory", lambda: tmp_path / "cache")
    result = CliRunner().invoke(main, ["--clear-cache"])
    assert result.exit_code == 0
    assert "キャッシュをクリアしました" in result.output
```

`test_clear_cache_clears_and_exits` は API キーを消しても成功することで
「バックエンド生成より先に判定・API キー不要」を検証する。

- [ ] **Step 2: テストが失敗することを確認する**

Run: `uv run pytest tests/test_cli.py -v`
Expected: FAIL(`--no-cache` / `--clear-cache` オプションが存在せず
`no such option` で exit_code 2、`captured` が空で KeyError など)

- [ ] **Step 3: main.py を実装する**

`src/yak/main.py` を以下の内容に更新する(全文):

```python
#!/usr/bin/env python

import os
import sys

import click
from openai import OpenAI

from yak.backends.base import DictionaryProvider
from yak.backends.openai import DEFAULT_MODEL, OpenAIBackend
from yak.cache import CachingBackend, clear_cache, open_cache
from yak.errors import YakError
from yak.interactive import InteractiveSession, run_interactive
from yak.render import render_dictionary


def create_backend(model: str, *, read_cache: bool = True) -> CachingBackend:
    api_key = os.environ.get("OPENAI_API_KEY_FOR_YAK")
    if not api_key:
        raise YakError("environment variable OPENAI_API_KEY_FOR_YAK is not set")
    inner = OpenAIBackend(OpenAI(api_key=api_key), model)
    return CachingBackend(
        inner,
        open_cache(),
        namespace=f"openai:{model}",
        read_enabled=read_cache,
    )


@click.command()
@click.option("--from", "-f", "from_lang", default=None, help="Source language")
@click.option("--to", "-t", "to_lang", default=None, help="Target language")
@click.option("--dictionary", "-d", is_flag=True, help="Dictionary mode")
@click.option(
    "--model", "-m", default=DEFAULT_MODEL, show_default=True, help="OpenAI model"
)
@click.option(
    "--no-cache", is_flag=True, help="Bypass cache reads (results are still saved)"
)
@click.option(
    "--clear-cache", "clear_cache_flag", is_flag=True, help="Clear the cache and exit"
)
@click.argument("text", required=False)
def main(
    from_lang: str | None,
    to_lang: str | None,
    dictionary: bool,
    model: str,
    no_cache: bool,
    clear_cache_flag: bool,
    text: str | None,
) -> None:
    """Translate TEXT (or stdin) with OpenAI."""
    try:
        if clear_cache_flag:
            count = clear_cache()
            click.echo(f"キャッシュをクリアしました ({count} 件)")
            return
        if text is None:
            if sys.stdin.isatty():
                backend = create_backend(model, read_cache=not no_cache)
                run_interactive(
                    InteractiveSession(
                        backend,
                        dictionary=dictionary,
                        from_lang=from_lang,
                        to_lang=to_lang,
                    )
                )
                return
            text = sys.stdin.read().strip()
        if not text:
            raise YakError("no input text")
        backend = create_backend(model, read_cache=not no_cache)
        if dictionary:
            if not isinstance(backend, DictionaryProvider):
                raise YakError("this backend does not support dictionary mode")
            click.echo(
                render_dictionary(backend.lookup(text, from_lang, to_lang, None))
            )
        else:
            click.echo(
                backend.translate(text, from_lang, to_lang, None).translated_text
            )
    except YakError as e:
        click.echo(f"yak: {e}", err=True)
        sys.exit(1)


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: テストが通ることを確認する**

Run: `uv run pytest tests/test_cli.py -v`
Expected: PASS(9 passed = 既存 6 + 新規 3)

Run: `uv run pytest`
Expected: 全テスト PASS

Run: `make check`
Expected: エラーなし

- [ ] **Step 5: README を更新する**

`README.md` のオプション表に 2 行追加:

```markdown
| `--no-cache` | キャッシュを読まずに翻訳(結果は保存される) |
| `--clear-cache` | キャッシュを全削除して終了 |
```

「対話モード」セクションの後に以下のセクションを追加:

```markdown
### キャッシュ

翻訳・辞書結果は `platformdirs` の示すユーザーキャッシュディレクトリ
(Linux では `~/.cache/yak`)に diskcache で永続キャッシュされる
(上限 100MB、TTL なし)。同じ入力・言語・モデルの再実行は API を呼ばない。
```

- [ ] **Step 6: コミット**

```bash
git add src/yak/main.py tests/test_cli.py README.md
git commit -m "feat: add cache CLI options (--no-cache, --clear-cache)"
```

---

### Task 3: 最終確認

**Files:**
- なし(検証のみ。問題があれば該当タスクの範囲で修正)

- [ ] **Step 1: 全体チェック**

Run: `make test`
Expected: mypy / ruff / pytest すべて成功

- [ ] **Step 2: 実 API での動作確認(手動・任意)**

`OPENAI_API_KEY_FOR_YAK` が設定された環境で:

```bash
uv run yak hello          # 1 回目: API 呼び出し
uv run yak hello          # 2 回目: 即座に返る(キャッシュヒット)
uv run yak --clear-cache  # 「キャッシュをクリアしました (N 件)」
```

Expected: 2 回目が体感で速い。API キーがない環境ではスキップしてよい。

- [ ] **Step 3: 未コミットの修正がないことを確認**

```bash
git status   # クリーンであることを確認
```
